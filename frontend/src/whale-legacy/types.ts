/**
 * Kalshi Whale Bot Dashboard Types (Legacy)
 * Re-exported from main types for backwards compatibility
 */

export type {
  MarketState,
  WhaleEvent,
  RegimeState,
  BotOverview,
  TimelineEvent,
  BotState,
} from "../types";
